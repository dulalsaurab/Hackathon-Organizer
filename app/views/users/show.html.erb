<% provide(:title, @user.user_name) %>
<div class="row">
  <aside class="col-md-4">
    <section>
      <h1>
        <%= @user.user_name %>
      </h1>
    </section>
    <section class="stats">
      <%= render 'common/stats' %>
    </section>
  </aside>
  <div class="col-md-8">
    <%= render 'follow_form' if logged? %>
  </div>
</div>